import { NextPage } from 'next';

import MessagesView from '@/components/organisms/MessagesView/MessagesView';

const RoomMessages: NextPage = () => {
  return <MessagesView />;
};

export default RoomMessages;
